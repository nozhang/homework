apiVersion: v1
kind: Service
metadata:
  labels:
    app: httpserver
  name: httpserver-service
  namespace: noel-test
spec:
  selector:
    app: httpserver
  ports:
  - protocol: TCP
    port: 8080
    targetPort: http
  type: NodePort
